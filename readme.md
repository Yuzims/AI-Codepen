按照我的要求：
1.AI集成：
方案三（AI代码生成）→ 方案一（对话助手）→ 方案二（错误修复）
在实现方案二之前，修改一下现在的代码错误检测，现在在用户每次连续输入的时候会弹出一个错误信息，用户体验不是很好，并且直接返回的错误信息不够直观，修改为类似vscode这类编辑器的错误检测的那种ui
2.前端性能监控SDK:
按照你上述提到的最合理的方案实现
3.可衡量的性能指标
要能够在简历上凸显性能优化前后的区别，已知现在页面的LCP是3.44s，有很大的优化空间
结合以上要求以及刚才的上下文，分别编写可以在不同上下文独立开发每个任务的开发文档，如果有业务重叠部分，则等到以上三部分内容都基本实现之后再进行整合